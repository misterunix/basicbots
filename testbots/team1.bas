10 LET MSG = ""
11 LET SCANANGLE = 0
12 LET DISTANCE = 0

100 LET DISTANCE = SCAN SCANANGLE,2
110 IF DISTANCE > 0 AND DISTANCE < 700 THEN GOSUB 1000
120 LET SCANANGLE = SCANANGLE + 1
121 IF SCANANGLE > 360 THEN LET SCANANGLE = SCANANGLE - 360
122 IF SCANANGLE < 0 THEN LET SCANANGLE = SCANANGLE + 360
130 GOTO 100


1000 LET LX = LOCX
1005 LET LY = LOCY
1010 LET X = ((COS SCANANGLE) * DISTANCE) + LX
1012 LET Y = ((SIN SCANANGLE) * DISTANCE) + LY
1026 PRINT "sa=",SCANANGLE," ","distance=",DISTANCE," "
1034 PRINT "x=",X," ","y=",Y,"\n"
1040 RETURN
